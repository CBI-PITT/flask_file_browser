# Agent Notes

- 2026-06-15: Updated the file browser selection payload to include `nextBrowserPath` so parent forms can reopen future pickers in the current folder or selected folder.
- 2026-09-08: Added configurable BrAinPI links to file modals for `.tif`/`.tiff`/`.ims` and folder modals for `.ome.zarr`/`.omehans`, gated by `[brainpi] enabled` and built from the existing real-path lookup.
- 2026-09-22: Added a chrome-less embed mode for picker iframes: extracted fl_browse_table_dir.html body and scripts into fl_browse_table_body.html / fl_browse_table_scripts.html (full page now includes them, no visual change) and added fl_browse_table_dir_embed.html plus /dir_embed/ routes in fs_browse.py (browse_fs_embed mirrors browse_fs with get_path_data("/dir_embed/"), same auth and plugin modal/trigger scanning; initiate_NOT_browseable registers a 404 version). All generated nav/download/get_file_path links stay self-consistent under /dir_embed/ because URL building is parameterized by the route base.
- 2026-09-22: Fixed the broken file/folder modals caused by the embed refactor: fl_browse_table_scripts.html was missing the closing `</script>` for its main inline JS block, so the whole script (show.bs.modal population of file name/size/modtime, Select File/Folder handlers, DataTable init) never executed in both the embed page and the full-page /dir/ view. Appended the missing `</script>`; verified both templates render with balanced script tags via offline Jinja renders.
- 2026-09-22: Fixed the embed picker's file/folder modals not opening after the asset redesign. The old shared scripts partial loaded the Bootstrap 5.3 bundle as its first line; the redesign moved the bundle into browser_base.html (full page) and dropped it from the partial, but the embed page (fl_browse_table_dir_embed.html) does not extend browser_base.html and loaded only CSS + jQuery, so inside the iframe the data-bs-toggle modal buttons were silently inert (no console error, no network) and data-bs-dismiss/Select bootstrap.Modal calls were also dead. Added the bundle to the embed head; verified offline that both the embed and full-page renders now ship exactly one bootstrap bundle + one jQuery + one DataTables with balanced script tags, and the embed stays chrome-less.
- 2026-09-23: Added a render/static pytest suite at tests/ (conftest.py + test_browser_contracts.py, pytest.ini with testpaths=tests; 11 tests) guarding the browser-side picker contracts: embed + full-page asset sets (exactly one bootstrap bundle / jQuery / DataTables per page, balanced script tags, no legacy assets), raw-template script-tag balance, modal trigger buttons with valid data-fdata JSON and all modal contract IDs, inline JS contracts in fl_browse_table_scripts.html (show.bs.modal, get_file_path dynamicLink, getFieldId/frameElement, postMessage payload, modal population ids), chrome-less embed, route-base consistency against fs_browse.py, brainpi anchors. pytest installed into peace-flask-test; mutation-verified that dropping the </script> tag or the embed bundle fails the suite. Run with `python3 -m pytest` from the repo root.
- 2026-09-23: Made Select File / Select Folder picker-mode only. The embed template sets `{% set picker_mode = true %}` before including the shared body/scripts partials; fl_browse_table_body.html renders the two Select buttons inside `{% if picker_mode %}` so they disappear from the full-page /browser/dir/ modals (where they dead-ended with 'Missing file path or field ID') while remaining in the picker iframe. fl_browse_table_scripts.html now null-guards the select-button listener attachments and the selpath/folderselpath dataset assignments (the shared JS previously would throw on the full page), and the file show handler stores the dynamicLink on the modal element itself; modals/multiscale.html reads the imaris_info link from #brainModal's dataset instead of the Select button so Multiscale Info keeps working in both contexts. Download/BrAinPI/Multiscale/Close remain in both. test_browser_contracts.py grew to 13 tests: test_select_buttons_only_in_picker_mode (embed has the buttons, full page does not, modal otherwise identical), guard markers in the inline JS contracts, and a multiscale-modal source check. Both suites verified live: full page renders 0 Select buttons, embed renders 2.
- 2026-09-23: Moved the breadcrumbs inline into the main navbar row (left, GitHub-style). browser_base.html dropped the `.ffb-navbar-path` sub-bar wrapper below the navbar and renders the breadcrumb block between the brand and the toggler; fl_browse_full_page.html adds an `ffb-breadcrumb-wrap` scroll container; browser.css replaced the strip rules with `flex: 1 1 auto; min-width: 0; overflow-x: auto;` + hidden scrollbar (long paths scroll horizontally, `flex-wrap: nowrap`); dark-navbar crumb colors unchanged. Bonus: login/home/profile no longer render the empty artifact strip (they use the empty breadcrumb block). test_browser_contracts.py grew to 14 tests with test_breadcrumb_inline_in_navbar (no sub-bar element, brand < breadcrumb < nav-links order, single home crumb at root, base template ships no strip). Verified live: 0 ffb-navbar-path on /browser/dir/ and /browser/login, breadcrumb inside the navbar row.
- 2026-09-23: Made the navbar and footer span the full viewport with a uniform gutter. Root cause: browser_base.html wrapped navbar/footer content in Bootstrap's `container-xxl` (max-width 1320px, centered), so on wide monitors the logo/logout sat near the center while the container-less file table ran edge to edge. Both containers are now `container-fluid ffb-container`, and browser.css gives `.ffb-main` the same 1rem horizontal gutter as `.ffb-container` so navbar, content, and footer edges all align (card corners get breathing room; embed-mode compact padding unaffected since the embed page does not use .ffb-main). test_browser_contracts.py grew to 15 tests with test_navbar_full_width (no container-xxl in base template, both containers container-fluid, content gutter pinned in browser.css). Verified live: 0 container-xxl on /browser/dir/ and /browser/login, 2 container-fluid ffb-container divs.
- 2026-09-22: Complete UI/UX redesign (light theme, Bootstrap 5.3 + Bootstrap Icons; no route/contract changes). New browser_base.html is now the standalone base (single dark navbar with CBI logo + breadcrumb path + Home/Queue/Login-Logout; fl_browse_full_page.html extends it; ffbbase.html is a thin wrapper for login/home/profile). New static/flask_file_browser/styles/browser.css holds the design tokens and component styles; brain-styles.css and vendored bootstrapv5.1.3.css/js are no longer linked. fl_browse_table_body.html rebuilt: toolbar chips (folder/file counts, updated time), Name/Last modified/Size/Actions columns with per-extension Bootstrap icons, data-order attributes for folders-first and byte-size sorting (sort-hack spans removed), redesigned file/folder modals with identical IDs, data-fdata and postMessage contracts. fl_browse_table_scripts.html keeps all JS logic with real DataTable options (searching/pageLength/order/language empty states) replacing no-op searchable/fixedHeight. Embed template loads the new CSS set and stays chrome-less with compact spacing. login/extended_home/profile converted from dead Bulma classes to Bootstrap cards; the login flash message previously sat outside any block and never displayed. Also fixed the brand logo URL (was /browser/static/images/... which 404s; now static/flask_file_browser/images/...) and fs_browse.py time_format %H:%I -> %H:%M (minutes were showing the 12-hour hour).
- 2026-09-23: P0 security fixes after the audit (traversal was empirically confirmed: GET /browser/dir/cbi/Iana/%2e%2e/%2e%2e/%2e%2e/%2e%2e/etc/hostname returned the file to an authenticated user, and get_file_path resolved arbitrary paths anonymously). utils.py `from_html_to_path` now rejects `.`/`..` segments and enforces realpath containment inside the mapped root (symlinks included, PermissionError on escape); `get_html_split_and_associated_file_path` honors real auth instead of forcing user_authenticated=True. routes.py: `file_path` + `imaris_info` now require login and return 403 JSON on escape (previously anonymous + 500/leak); fs_browse.py `browse_fs_json` requires login. auth.py: `login_manager.login_view` fixed to 'flask_file_browser.login' (was 'login', which raised BuildError -> 500 on every login redirect — a latent bug the new tests exposed). New tests `tests/test_security_containment.py` (10 tests; conftest gained security_app/security_client/security_login fixtures): traversal + symlink rejection, login required on all three JSON routes, 403 on escape, inside-root still works, non-owner dir download blocked. Browser suite now 25 passed. Verified live: anonymous get_file_path/dir_json/traversal -> 302.
